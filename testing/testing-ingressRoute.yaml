apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sitemaps
  namespace: default
spec:
  entryPoints:
    - websecure
  tls:
    certResolver: myresolver
  routes:
    # sitemaps
    - match: Host(`testing.pocketcoach.fit`)
      kind: Rule
      services:
        - name: whoami
          port: 80
